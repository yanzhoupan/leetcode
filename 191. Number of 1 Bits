class Solution(object):
    def hammingWeight(self, n):
        """
        :type n: int
        :rtype: int
        """
        n_list = list(str(bin(n)))
        sum = 0
        for i in n_list:
            if i == '1':
                sum += 1
        return sum
