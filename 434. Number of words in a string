class Solution(object):
    def countSegments(self, s):
        """
        :type s: str
        :rtype: int
        """
        sList = s.split(' ')
        res = 0
        for i in sList:
            if len(i) > 0:
                res += 1
        return res
